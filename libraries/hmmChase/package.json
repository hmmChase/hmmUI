{
  "name": "@hmmui/hmmchase",
  "description": "hmmchase",
  "version": "1.0.3",
  "license": "ISC",
  "author": "hmmChase",
  "main": "dist/index.js",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "cross-env webpack -p --config ../../webpack.config.js",
    "clean": "rimraf dist && rimraf node_modules && rimraf package-lock.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "peerDependencies": {
    "react": "^16.13.0",
    "react-dom": "^16.13.0"
  }
}
